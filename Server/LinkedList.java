package Server;

public class LinkedList<T> {
	private LLNode<T> first;
	private LLNode<T> last;
	
	public LinkedList()
	{
		first = null;
		last = null;
	}
	
	public void add(T value)
	{
		if(isEmpty()==true)
			first = new LLNode<T>(e)
	}
	public boolean isEmpty()
	{
		if(first.equals(last))
			return true;
		else
			return false;
	}
	

}
